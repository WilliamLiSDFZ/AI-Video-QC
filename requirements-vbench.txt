# VBench 依赖清单（供 Apple Silicon 上 `pip install vbench --no-deps` 后手动补齐用）
# = VBench 官方 requirements.txt 去掉 decord、换成 eva-decord
# 正常 Linux/CUDA 机器直接 `pip install vbench` 即可，无需本文件
eva-decord
Pillow
numpy<2.0.0
matplotlib
timm>=0.9,<=1.0.12
wheel
cython
tensorboard
scipy
opencv-python
scikit-learn
scikit-image
openai-clip
requests
pyyaml
easydict
pyiqa
lvis
fairscale>=0.4.4
fvcore
urllib3
boto3
omegaconf
transformers==4.33.2
pycocoevalcap
